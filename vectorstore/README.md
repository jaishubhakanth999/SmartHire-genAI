# vectorstore/

Persisted FAISS artefacts (`index.faiss` + `index.pkl` metadata).

**Generated, not authored.** Built by `notebooks/02_build_faiss.ipynb` and by
the indexing helpers in `src/search/embed.py`. Contents are gitignored so the
repository stays small and the index is always rebuildable from `data/`.
